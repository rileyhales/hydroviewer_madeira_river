version: 1.0
name: hydroviewer_madeira_river

requirements:
  skip: false
  conda:
    channels:
      - conda-forge
    packages:
      - geoglows
      - hydrostats
      - pandas
      - requests
      - plotly
      - numpy
      - scipy
      - bs4
      - lxml
